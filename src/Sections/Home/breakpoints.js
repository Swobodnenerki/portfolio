const size = {
    xs: '0px',
    s: '480px',
    sm: '950px',
   }
   const device = {
    xs: `(min-width: ${size.xs})`,
    s: `(min-width: ${size.s})`,
    sm: `(min-width: ${size.sm})`
   }
export default {size, device}